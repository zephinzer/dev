/*
	Package system implements utility methods that should apply
	across all operating systems and can be called in a cross-platform
	compatible manner.

	At the minimum level, Ubuntu (Linux), MacOS, and Windows should be supported.

	Why Ubuntu? Because I use it.
*/
package system
